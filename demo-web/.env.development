# Để trống: gọi /api qua Vite proxy → http://127.0.0.1:5000 (ổn khi mở dev bằng localhost hay --host).
# Chỉ đặt khi bạn cần gọi thẳng Flask, ví dụ:
# VITE_API_BASE=http://127.0.0.1:5000
VITE_API_BASE=
