# Empty: browser calls /api/*; Vite proxies to Flask (see vite.config.js, default :5000).
# VITE_API_BASE=http://127.0.0.1:5000
VITE_API_BASE=
# Override text API root only if text routes are not under the same host as /api.
# VITE_API_TEXT_BASE=http://127.0.0.1:5000
VITE_API_TEXT_BASE=
